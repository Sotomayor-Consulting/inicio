---

interface Props {
  title?: string;
}

const baseClasses =
  "flex items-center gap-x-2 text-base md:text-sm font-medium text-primario-black ring-zinc-500 transition duration-300 focus-visible:ring-3 outline-hidden";
const hoverClasses = "hover:text-secundario dark:hover:text-secundario";
const darkClasses =
  "dark:border-neutral-700 dark:text-neutral-400 dark:ring-zinc-200 dark:focus:outline-hidden";
const mdClasses = "md:my-6 md:border-s md:border-neutral-300 md:ps-6";
const txtSizeClasses = "2xl:text-base";
const userSVG = `<svg
      class="h-4 w-4 shrink-0"
      width="24"
      height="24"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      stroke-width="2"
      stroke-linecap="round"
      stroke-linejoin="round"
  >
    <path d="M19 21v-2a4 4 0 0 0-4-4H9a4 4 0 0 0-4 4v2"></path>
    <circle cx="12" cy="7" r="4"></circle>
  </svg>`;

const pathname = Astro.url.pathname;
const isEnglish = pathname.startsWith("/en");
const isPortuguese = pathname.startsWith("/pr");
const UrlPartners = isEnglish ? "https://app.sotomayorconsulting.com/sign-in" : isPortuguese ? "https://app.sotomayorconsulting.com/sign-in" : "https://app.sotomayorconsulting.com/sign-in";
const title = isEnglish ? "Log In" : isPortuguese ? "Fazer Login" : "Iniciar Sesión";
---

<a
  href={UrlPartners}
  class={`${baseClasses} ${hoverClasses} ${darkClasses} ${mdClasses} ${txtSizeClasses}`}
>
  <!-- About Fragment: https://docs.astro.build/en/basics/astro-syntax/#fragments -->

  <Fragment set:html={userSVG} />
  {title}
</a>
